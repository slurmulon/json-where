{
  "name": "json-rel",
  "version": "0.0.5",
  "description": "Transparent references in JSON",
  "main": "lib/index.js",
  "scripts": {
    "build": "node ./node_modules/.bin/babel src --out-dir lib",
    "build-maps": "node ./node_modules/.bin/babel src --out-dir lib --source-maps",
    "lint": "eslint src/index.js",
    "start": "./node_modules/.bin/babel-node src/index.js",
    "pretest": "npm run build",
    "test": "node ./node_modules/.bin/mocha --reporter nyan --compilers js:babel-core/register test",
    "auto-start": "nodemon --exec \"npm run lint && npm start\" --ignore public/js",
    "prepublish": "npm run build",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --compilers js:babel-core/register"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/slurmulon/json-rel.git"
  },
  "keywords": [
    "json",
    "rel",
    "relation",
    "reference",
    "hyper",
    "hypermedia",
    "jsonpath",
    "jsonpointer",
    "path",
    "pointer",
    "query",
    "find"
  ],
  "author": "slurmulon",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/slurmulon/json-rel/issues"
  },
  "homepage": "https://github.com/slurmulon/json-rel#readme",
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-preset-es2015": "^6.6.0",
    "blanket": "^1.2.3",
    "chai": "^3.5.0",
    "chai-things": "^0.2.0",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "json-query": "^1.8.0",
    "jsonpath": "^0.2.2",
    "jsonpointer": "^3.0.0"
  }
}
